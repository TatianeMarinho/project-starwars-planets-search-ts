🪐 Star Wars Planet Search (TS)

Uma ferramenta de busca avançada para o universo Star Wars. A aplicação consome a API de planetas e permite que o usuário realize filtragens complexas por nome e dados numéricos (população, diâmetro, período orbital, etc.).

🚀 Funcionalidades Principais

Filtros Combinados: Possibilidade de aplicar múltiplos filtros numéricos simultâneos (ex: população maior que X e diâmetro menor que Y).

Gerenciamento de Estado Global: Implementado com Context API e React Hooks.

Ordenação: Funcionalidade para ordenar os planetas de forma crescente ou decrescente baseada em diferentes colunas.

Exclusão de Filtros: Opção para remover filtros aplicados individualmente ou todos de uma vez.

🧰 Stack Técnica

React (Context API para estado global).

TypeScript (Tipagem rigorosa dos dados da API e dos estados de filtro).

Hooks (useState, useEffect, useContext).

Testes: Desenvolvido com foco em testes para garantir o funcionamento da lógica de filtragem.
